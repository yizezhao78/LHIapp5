import React from 'react';
import { FlatList, View, Text } from 'react-native';

const GiveScreen = props => {
    return (<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
        <Text>GiveScreen</Text>
        </View>
        );
}

export default GiveScreen;